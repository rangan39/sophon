[project]
name = "sophon-interp-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "modal>=1.0.0",
  "pydantic>=2.8.0",
  "torch>=2.4.0",
  "transformer-lens>=2.16.0",
  "uvicorn[standard]>=0.30.0"
]

[tool.pyright]
venvPath = "."
venv = ".venv"
